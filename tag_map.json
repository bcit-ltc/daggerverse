{
    "review": {
        "rules": [
            { "format": "{env}-{version}-{commit_hash}.{timestamp}" }
        ]
    },
    "latest": {
        "rules": [
            { "format": "{version}-{date}-{commit_hash}.{timestamp}" },
            { "format": "{env}" }

        ]
    },
    "stable": {
        "rules": [
            { "format": "{version}" },
            { "format": "{version}-{date}-{commit_hash}.{timestamp}" },
            { "format": "latest" },
            { "format": "{env}" }
        ]
    }
}